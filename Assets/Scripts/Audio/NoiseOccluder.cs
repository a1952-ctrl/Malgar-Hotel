using UnityEngine;

namespace MalgarHotel.Audio
{
    /// <summary>
    /// Marker component used by the NoiseSystem to attenuate events that occur behind
    /// occluding geometry.
    /// </summary>
    public class NoiseOccluder : MonoBehaviour
    {
    }
}
